{
  "builds": [
    {
      "src": "server/app.js",
      "command": "npm run build",
      "framework": "node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/client/dist/$1"
    }
  ]
}